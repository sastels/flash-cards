/** @jsx jsx */
import { jsx } from '@emotion/core';
import { H1, CenterContent } from '@cdssnc/repertoire';

const FlashCards = () => (
  <CenterContent mt={[1, 1, 6]}>
    <H1 textAlign="center">Flash cards rough in</H1>
  </CenterContent>
);
export default FlashCards;
